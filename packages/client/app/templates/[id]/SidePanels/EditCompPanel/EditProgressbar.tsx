import { ProgressBarCompProps } from "@asius/types";
import { EditSection } from "./EditSection";
import { SetComp } from "./index";

export const EditProgressbar = ({
  comp,
  setComp,
}: {
  comp: ProgressBarCompProps;
  setComp: SetComp;
}) => {
  return <EditSection title="Progressbar">a</EditSection>;
};
