# @asius/player

Let's you include Video that you hvae created on your website.

## Installation

import { Tab, Tabs } from "nextra-theme-docs";

<Tabs items={["yarn", "npm", "pnpm"]}>
      <Tab>
        ```bash copy 
        yarn add @asius/player
        ```
      </Tab>
      <Tab>
        ```bash copy 
        npm install @asius/player 
        ```
        </Tab>
      <Tab>
        ```bash copy 
        pnpm add @asius/player 
        ```
      </Tab>
    </Tabs>

