# @asius/sdk

For rendering videos and images.

## Installation

import { Tab, Tabs } from "nextra-theme-docs";

<Tabs items={["yarn", "npm", "pnpm"]}>
      <Tab>
        ```bash copy 
        yarn add @asius/sdk 
        ```
      </Tab>
      <Tab>
        ```bash copy 
        npm install @asius/sdk 
        ```
        </Tab>
      <Tab>
        ```bash copy 
        pnpm add @asius/sdk 
        ```
      </Tab>
    </Tabs>

## Usage
