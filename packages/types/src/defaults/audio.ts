import { AudioCompProps, videoUrl } from "..";

export const defaultAudioProps: AudioCompProps = {
    id: "",
    type: "audio",
    height: 500,
    width: 500,
    x: 0,
    y: 0,
    borderRadius: 10,
    rotation: 0,
    startFrom: 0,
    src: videoUrl,
    from: 0, duration: 0,
    volume: 1,
}
