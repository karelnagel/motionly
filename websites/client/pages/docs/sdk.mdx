# @motionly/sdk

For rendering videos and images.

## Installation

import { Tab, Tabs } from "nextra-theme-docs";

<Tabs items={["yarn", "npm", "pnpm"]}>
  <Tab>```bash copy yarn add @motionly/sdk ```</Tab>
  <Tab>```bash copy npm install @motionly/sdk ```</Tab>
  <Tab>```bash copy pnpm add @motionly/sdk ```</Tab>
</Tabs>

## Usage
