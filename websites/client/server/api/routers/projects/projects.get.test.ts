import { testUnauthorized } from "../../testHelpers";
import { test } from "@jest/globals";
const input = {
  id: "123",
};
test("Should fail if not owner and private", async () => {});
test("Should fail if not public", async () => {});
test("Should return the project when owner", async () => {});
test("Should return the project when public and not authorized", async () => {});
