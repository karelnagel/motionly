import { testUnauthorized } from "../../testHelpers";
import { test } from "@jest/globals";
const input = {
  id: "123",
};
test("Should fail if getting id that has user", () => {});
test("Should return render and update progress when unauthorized", async () => {});
test("Should return render and update progress when authorized", async () => {});
