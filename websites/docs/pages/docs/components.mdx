# @motionly/components

Remotion components that are used inside Motionly, but that could be used in any other Remotion project.

## Installation

import { Tab, Tabs } from "nextra-theme-docs";

<Tabs items={["yarn", "npm", "pnpm"]}>
  <Tab>```bash copy yarn add @motionly/components ```</Tab>
  <Tab>```bash copy npm install @motionly/components ```</Tab>
  <Tab>```bash copy pnpm add @motionly/components ```</Tab>
</Tabs>
